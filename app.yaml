application: arched-flame-89818
version: 1
runtime: python27
api_version: 1
threadsafe: yes

handlers:
- url: /favicon\.ico
  static_files: favicon.ico
  upload: favicon\.ico

- url: /.*
  script: backend.wsgi.application  # a WSGI application in the main module's global scope


libraries:
  - name: django
    version: "1.5"
