'use strict';

describe('myApp.profileBuilder module', function() {

  beforeEach(module('myApp.profileBuilder'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ProfileCtrl');
      expect(HomeCtrl).toBeDefined();
    }));

  });
});
