'use strict';

describe('myApp.deadlines module', function() {

  beforeEach(module('myApp.deadlines'));

  describe('Deadlines controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('DeadlinesCtrl');
      expect(HomeCtrl).toBeDefined();
    }));

  });
});